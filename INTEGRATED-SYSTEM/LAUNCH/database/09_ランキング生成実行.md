# 09 ランキング生成実行

**対象**: 8種類データベースの多軸評価ランキング生成  
**手法**: TENスコア算出・Node.js自動ランキング生成  
**出力**: 全データベースの評価付きランキングJSON  
**保存先**: `app/data/[各データベース]/rankings/`

---

## 🚀 基本実行コマンド

```bash
【ランキング生成実行】

# 企業ランキング生成（114パターン）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/companyDatabase
node generateAllRankings.js

# AIツールランキング生成（TEN 6軸評価）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/aiToolsDatabase
node generateCompleteRankingsV7.js

# 資格ランキング生成（TEN 6軸評価）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/qualificationDatabase
node generateQualificationRankings.js

# ガジェットランキング生成（TEN 6軸評価）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/gadgetDatabase
node generateGadgetRankings.js

# ソフトウェアランキング生成（TEN 6軸評価）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/softwareDatabase
node generateSoftwareRankings.js

# モバイルアプリランキング生成（TEN 6軸評価）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/mobileAppDatabase
node generateAppRankings.js

# 習慣ランキング生成（TEN 6軸評価）
cd /mnt/c/instagram-course/instagram-post-generator/app/data/habitDatabase
node generateHabitRankings.js

# 横断ランキング生成（KIKUYO・TEN用）
cd /mnt/c/instagram-course/instagram-post-generator/app/data
node generateCrossRankings.js
```

---

## 📋 ランキング生成対象（8データベース）

| # | データベース | 評価方式 | 生成パターン数 | 主要ランキング例 |
|---|-------------|---------|---------------|-----------------|
| **1** | **企業DB** | 業界・条件別 | 114パターン | 初任給TOP10, 働きやすさTOP15, 年収ランキング |
| **2** | **AIツールDB** | TEN 6軸 | 20パターン | 総合TOP20, カテゴリ別, コスパランキング |
| **3** | **資格DB** | TEN 6軸 | 15パターン | 取りやすさTOP10, キャリア効果TOP15 |
| **4** | **ガジェットDB** | TEN 6軸 | 15パターン | 生産性TOP10, コスパTOP15, 人気ランキング |
| **5** | **ソフトウェアDB** | TEN 6軸 | 12パターン | 使いやすさTOP10, 機能TOP10 |
| **6** | **モバイルアプリDB** | TEN 6軸 | 10パターン | 習慣化TOP10, 学習効果TOP10 |
| **7** | **習慣DB** | TEN 6軸 | 12パターン | 続けやすさTOP10, 効果実感TOP15 |
| **8** | **横断ランキング** | 8DB統合 | 25パターン | 生産性向上総合TOP20, 初心者おすすめTOP15 |

---

## 🔧 TEN評価スコア算出仕様

### **共通TEN評価算出式**
```javascript
// TENスコア算出ロジック (各データベース共通)

function calculateTENScore(item) {
  const weights = {
    immediacy: 0.20,        // 即効性 20%
    simplicity: 0.20,       // 簡単さ 20%  
    popularity: 0.15,       // 人気度 15%
    costPerformance: 0.15,  // コスパ 15%
    specialization: 0.20,   // 機能専門性 20%
    productivityGain: 0.10  // 生産性UP度 10%
  };

  // 各軸スコア（0-100点）
  const scores = {
    immediacy: evaluateImmediacy(item),
    simplicity: evaluateSimplicity(item),
    popularity: evaluatePopularity(item),
    costPerformance: evaluateCostPerformance(item),
    specialization: evaluateSpecialization(item),
    productivityGain: evaluateProductivityGain(item)
  };

  // 重み付き総合スコア計算
  const tenScore = Object.keys(weights).reduce((total, key) => {
    return total + (scores[key] * weights[key]);
  }, 0);

  return {
    tenScore: Math.round(tenScore * 10) / 10, // 小数点1位
    breakdown: scores,
    weights: weights,
    grade: getGrade(tenScore)
  };
}

// グレード判定
function getGrade(score) {
  if (score >= 95) return 'S+';
  if (score >= 90) return 'S';
  if (score >= 85) return 'A+';
  if (score >= 80) return 'A';
  if (score >= 75) return 'B+';
  if (score >= 70) return 'B';
  if (score >= 65) return 'C+';
  if (score >= 60) return 'C';
  return 'D';
}
```

---

## 📊 ランキングパターン仕様

### **AIツールDB - 20パターン例**
```javascript
const rankingPatterns = [
  {
    id: 'AT001',
    name: '生産性向上AI総合TOP20',
    description: 'TENスコア総合評価による生産性AIツールランキング',
    criteria: { tenScore: 'desc' },
    limit: 20
  },
  {
    id: 'AT002', 
    name: 'コスパ最強AIツールTOP15',
    description: 'コストパフォーマンス重視のAIツール選定',
    criteria: { costPerformance: 'desc' },
    limit: 15
  },
  // ... 他18パターン
];
```

### **企業DB - 114パターン例**
```javascript
const companyRankingPatterns = [
  // 就活生向け (JS001-JS030)
  {
    id: 'JS001',
    name: '初任給ランキングTOP10',
    targetPersona: 'jobSeekers',
    criteria: { startingSalary: 'desc' }
  },
  // 女性キャリア向け (FC001-FC030) 
  {
    id: 'FC001',
    name: 'ワークライフバランス企業TOP10',
    targetPersona: 'femaleCareer',
    criteria: { workLifeBalance: 'desc' }
  },
  // 男性社会人向け (MP001-MP030)
  {
    id: 'MP001',
    name: '年収ランキングTOP10',
    targetPersona: 'maleProfessional', 
    criteria: { averageSalary: 'desc' }
  },
  // 新業界別ランキング (JS031-JS038, FC031-FC038, MP031-MP038)
  // ... 合計114パターン
];
```

---

## 🚀 実行手順

### **STEP1: TENスコア算出・評価**
```bash
# 各データベースのTENスコア算出
echo "TENスコア算出開始..."

# AIツール (77ツール)
cd app/data/aiToolsDatabase
node calculateTENScores.js

# 資格 (70+資格)  
cd ../qualificationDatabase
node calculateTENScores.js

# ガジェット (70+ガジェット)
cd ../gadgetDatabase
node calculateTENScores.js

# ソフトウェア (60+ソフトウェア)
cd ../softwareDatabase  
node calculateTENScores.js

# モバイルアプリ (50+アプリ)
cd ../mobileAppDatabase
node calculateTENScores.js

# 習慣 (60+習慣)
cd ../habitDatabase
node calculateTENScores.js

echo "TENスコア算出完了"
```

### **STEP2: 個別ランキング生成**
```bash
# 各データベースの個別ランキング生成
echo "個別ランキング生成開始..."

# 企業ランキング (114パターン)
cd app/data/companyDatabase
node generateAllRankings.js

# AIツールランキング (20パターン)  
cd ../aiToolsDatabase
node generateCompleteRankingsV7.js

# 資格ランキング (15パターン)
cd ../qualificationDatabase
node generateQualificationRankings.js

# ガジェットランキング (15パターン)
cd ../gadgetDatabase  
node generateGadgetRankings.js

# ソフトウェアランキング (12パターン)
cd ../softwareDatabase
node generateSoftwareRankings.js

# モバイルアプリランキング (10パターン)
cd ../mobileAppDatabase
node generateAppRankings.js

# 習慣ランキング (12パターン)
cd ../habitDatabase
node generateHabitRankings.js

echo "個別ランキング生成完了"
```

### **STEP3: 横断ランキング生成**
```bash
# KIKUYO・TEN用横断ランキング生成
echo "横断ランキング生成開始..."

cd app/data
node generateCrossRankings.js

echo "横断ランキング生成完了"
```

### **STEP4: 生成結果確認**
```bash
# ランキングファイル存在・数量確認
echo "=== ランキング生成結果確認 ==="

echo "企業ランキング:"
ls -1 app/data/companyDatabase/rankings*/ | wc -l
echo "AIツールランキング:"  
ls -1 app/data/aiToolsDatabase/rankingsV7/ | wc -l
echo "資格ランキング:"
ls -1 app/data/qualificationDatabase/rankings/ | wc -l
echo "ガジェットランキング:"
ls -1 app/data/gadgetDatabase/rankings/ | wc -l
echo "ソフトウェアランキング:"
ls -1 app/data/softwareDatabase/rankings/ | wc -l
echo "モバイルアプリランキング:"
ls -1 app/data/mobileAppDatabase/rankings/ | wc -l
echo "習慣ランキング:"
ls -1 app/data/habitDatabase/rankings/ | wc -l
echo "横断ランキング:"
ls -1 app/data/crossRankings/ | wc -l
```

---

## 🔧 品質管理

### **必須確認項目**
- [ ] 全8データベースのTENスコア算出完了
- [ ] 各データベースの想定ランキング数生成確認
- [ ] JSON構文エラー0件確認
- [ ] スコア算出ロジックの妥当性確認
- [ ] ランキング順位の正確性確認

### **データ品質基準**
- **完全性**: 全レコードのTENスコア算出100%
- **正確性**: ランキング順位計算エラー0件
- **一貫性**: 評価基準の統一性確保
- **最新性**: 最新統合DBでのランキング生成

---

## ⚠️ エラー対応

### **よくあるエラーと対処法**

#### **1. TENスコア算出エラー**
```
Error: Cannot calculate TEN score - missing evaluation data
```
**対処**: 統合DB内のevaluationフィールド確認・データ補完

#### **2. ランキング重複エラー**
```
Duplicate ranking ID found
```
**対処**: ランキングIDの一意性確認・重複排除

#### **3. 横断ランキング生成エラー**
```
Cross-database ranking failed - missing source data  
```
**対処**: 各データベースの統合完了確認・データ整合性チェック

---

## 🎯 成果目標

**ランキング生成目標**:
- **総ランキング数**: 233パターン以上の生成
- **TENスコア精度**: 95%以上の算出精度
- **JSON品質**: 構文エラー0件

**効率目標**:
- **実行時間**: 全ランキング生成30分以内
- **エラー率**: 1%以下
- **自動化率**: 100%自動生成

**活用準備**:
- **コンテンツ生成**: TYPE003・TYPE004対応準備完了
- **横断検索**: KIKUYO・TEN対応準備完了
- **動的更新**: 自動ランキング更新システム準備完了

---

## 🔗 参照ファイル

### **INTEGRATED-SYSTEM内参照**
- `INTEGRATED-SYSTEM/LAUNCH/database/08_統合DB生成実行.md` - 前ステップ
- `INTEGRATED-SYSTEM/LAUNCH/feed-posts/TYPE003_KIKUYO実行.md` - 企業ランキング活用
- `INTEGRATED-SYSTEM/LAUNCH/feed-posts/TYPE004_TEN実行.md` - 生産性ランキング活用

### **実行対象ディレクトリ**
- `app/data/companyDatabase/rankings*/` - 企業ランキング
- `app/data/aiToolsDatabase/rankingsV7/` - AIツールランキング
- `app/data/qualificationDatabase/rankings/` - 資格ランキング
- `app/data/gadgetDatabase/rankings/` - ガジェットランキング
- `app/data/softwareDatabase/rankings/` - ソフトウェアランキング
- `app/data/mobileAppDatabase/rankings/` - アプリランキング
- `app/data/habitDatabase/rankings/` - 習慣ランキング
- `app/data/crossRankings/` - 横断ランキング

### **元データ参照（参考のみ）**
- `ACTIVE-ROUTINES/01_DAILY_USE/database/04_ランキング生成起動術式.md` - 元仕様
- `app/data/companyDatabase/targetNeedsPatterns.js` - 企業ランキングパターン定義

---

**移行者**: 次世代Claude Code  
**移行日**: 2025-09-03  
**バージョン**: v1.0（統合ディレクトリ対応版）  
**基盤**: ランキング生成起動術式統合

**重要**: この統合起動術式により、8種類のデータベースから233+パターンのランキングを自動生成し、KIKUYO・TEN・TYPE003・TYPE004全キャラクターの高品質ランキングコンテンツ生成システムを完成できます。